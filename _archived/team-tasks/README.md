# team-tasks (архив)

Временно отключено из бота **2026-06-24**.

## Содержимое

| Путь | Было |
|---|---|
| `skill/` | `skills/team-tasks/` |
| `scripts/team-tasks.mjs` | `scripts/team-tasks.mjs` |
| `scripts/lib/team-tasks.mjs` | `scripts/lib/team-tasks.mjs` |

Данные пользователей (`data/teams/`, `users/*/teams.json`) **не трогались**.

## Вернуть в бота

1. `Move-Item skill ../skills/team-tasks` (из корня golden-hour)
2. Вернуть скрипты в `scripts/` и `scripts/lib/`
3. Восстановить ссылки в `help-menu`, `session-start`, `checkins`, `SOUL.md`, `run-tests.mjs`
